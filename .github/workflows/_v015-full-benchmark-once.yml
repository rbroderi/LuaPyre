name: 0.15 full benchmark once

on:
  push:
    branches:
      - v0.15-ast-superjit

permissions:
  contents: read

jobs:
  benchmark:
    uses: ./.github/workflows/benchmark-runtimes.yml
    with:
      repeats: '7'
      warmups: '3'
