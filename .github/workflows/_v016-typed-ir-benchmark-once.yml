name: 0.16 typed IR benchmark once

on:
  push:
    branches:
      - v0.16-typed-ir

permissions:
  contents: read

jobs:
  benchmark:
    uses: ./.github/workflows/benchmark-runtimes.yml
    with:
      repeats: '7'
      warmups: '3'
