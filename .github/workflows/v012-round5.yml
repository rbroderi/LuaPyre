name: apply 0.12 round-five semantic fixes

on:
  push:
    branches:
      - v0.12-official-suite-expansion

permissions:
  contents: write

jobs:
  apply:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v0.12-official-suite-expansion
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Apply patch
        run: python tools/_v012_round5.py
      - name: Commit semantic fixes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/luapyre/lexer.py src/luapyre/compiler.py src/luapyre/stdlib_string.py
          git commit -m 'Fix trailing zero lexing readonly upvalues and X alignment'
          git push origin HEAD:v0.12-official-suite-expansion
